import type { Meta, StoryObj } from '@storybook/react'
import { Tooltip } from '@felipecruz-ignite-ui/react'

export default {
  title: 'Data Display/Tooltip',
  component: Tooltip,
  args: {},
} as Meta

export const Primary: StoryObj = {}
